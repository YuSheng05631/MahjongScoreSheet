<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mahjongscoresheet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NameActivity" />
        <activity android:name=".RonActivity" />
        <activity android:name=".RichiActivity" />
        <activity android:name=".TsumoActivity" />
        <activity android:name=".RyuukyokuActivity" />
        <activity android:name=".Ron2Activity" />
        <activity android:name=".Ron2BActivity" />
        <activity android:name=".EndActivity" />
        <activity android:name=".PointTableActivity"></activity>
    </application>

</manifest>